services:
  postgres:
    container_name: postgres_go
    image: postgres:16.4
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: sqlpass
      PGDATA: /data/postgres
    volumes:
      - /home/sunrise/postgres-data:/data/postgres
    ports:
      - "5432:5432"
